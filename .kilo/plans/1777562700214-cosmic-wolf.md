# Plan for Fixing Image Cropping and Removing Pagination

## Problem Analysis
Based on the code review of `/Users/bread/Documents/code/Github/BAKERY_site/components/profile/profile-page.tsx`:

1. **Image Cropping Issue**: In the GalleryPanel component (lines 592-633), images are displayed using `object-cover` class which causes cropping to maintain aspect ratio. The aspect ratio container is set to `aspect-[16/9]` with minimum heights.

2. **Pagination Field to Remove**: The right-side panel containing:
   - Page counter: `{String(currentIndex + 1).padStart(2, "0")} / {String(section.items.length).padStart(2, "0")}` (lines 637-640)
   - Mobile navigation buttons (hidden on medium+ screens)

## Solution Approach

### 1. Fix Image Cropping
Modify the image display in GalleryPanel to prevent unwanted cropping:
- Change image fitting strategy from `object-cover` to `object-contain` 
- Or adjust the aspect ratio container to better fit the artwork
- Since some items already use `fit: "contain"` in their data, we should respect that property

### 2. Remove Pagination Field
Remove the right-side panel that contains:
- The page counter display
- Keep the mobile navigation buttons if still needed for UX, but remove the desktop pagination counter

## Implementation Details

### Changes to GalleryPanel component:

**For image cropping fix:**
In lines 619-624, modify the Image className to:
```typescript
className={cn(
  item.fit === "contain"
    ? "object-contain p-3 sm:p-4 md:p-5"
    : "object-contain p-3 sm:p-4 md:p-5", // Changed from object-cover to object-contain
)}
```

Alternatively, we could make it always use object-contain since the user mentioned images are being cropped.

**For pagination removal:**
Remove or comment out lines 635-653 which contain:
- The flex-col justify-between container
- The space-y-3 div with the page counter
- Keep the mobile navigation buttons if needed separately

However, looking more carefully, the mobile navigation buttons are in lines 644-651 which are inside `md:hidden`, so they only show on mobile. The user said to remove "右側的頁碼欄位" (right-side pagination field), which likely refers to the desktop counter only.

Let me verify this by checking if the mobile buttons should remain.

Actually, looking at the structure:
- Lines 582-590: Hidden on mobile, shows on medium+ (Previous/Next buttons)
- Lines 644-651: Shows on mobile, hidden on medium+ (Previous/Next buttons)

So the user likely wants to remove the desktop right-side panel (lines 635-653) which contains the pagination counter, while keeping the mobile navigation functionality intact.

## Files to Modify
- `/Users/bread/Documents/code/Github/BAKERY_site/components/profile/profile-page.tsx` - GalleryPanel component

## Testing Considerations
After changes, verify:
1. Images display without cropping in works and commission sections
2. Pagination counter is removed from desktop view
3. Mobile navigation still works correctly
4. Layout remains stable and visually appealing